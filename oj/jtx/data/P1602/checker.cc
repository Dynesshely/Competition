#include <bits/stdc++.h>
#include "testlib.h"
using namespace std;
int main(int argc, char * argv[]){
    setName("Count the number of correct chars");
    registerTestlibCmd(argc, argv);
    
    freopen("user_code", "r", stdin);
    
}
