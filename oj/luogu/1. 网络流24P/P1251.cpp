#include <bits/stdc++.h>
using namespace std;
int N, p, m, f, n, s;
int main(){
    scanf("%d", &N);
    int np[N];
    for(int i = 1; i <= N; ++ i) scanf("%d", &np[i]);
    scanf("%d%d%d%d%d", &p, &m, &f, &n, &s);
    
    return 0;
}
