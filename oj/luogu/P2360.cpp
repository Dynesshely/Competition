#include <bits/stdc++.h>
using namespace std;

const int maxn = 35;
int L, R, C, time = 0;
struct pos{ int x, y, z; }ps[3];
// 0 -> my pos; 1 -> start pos; 2 -> end pos;
char zone[maxn][maxn][maxn];
int main(){
    scanf("%d %d %d", &L, &R, &C);
    for(int i = 1; i <= L; ++ i)
        for(int r = 1; r <= R; ++ r)
            for(int c = 1; c <= C; ++ c){
                
            }
    return 0;
}