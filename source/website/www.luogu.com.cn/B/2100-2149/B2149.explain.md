# B2149 洛谷 求三角形面积 题解

## 题目链接
- [洛谷 B2149](https://www.luogu.com.cn/problem/B2149)

## 题目大意
给定三条线段长度 a, b, c（正实数，不超过 100），判断能否构成三角形。若能，使用海伦公式计算面积并保留两位小数输出；否则输出 `No solution.`。

## 解题思路
先检查三角形不等式：任意两边之和大于第三边。若成立，则用海伦公式 s = (a+b+c)/2，面积 = sqrt(s(s-a)(s-b)(s-c)) 计算面积。

## 代码实现
```cpp
#include <bits/stdc++.h>
using namespace std;

double a, b, c;

bool isTriangle(double a, double b, double c) { return a + b > c && a + c > b && b + c > a; }

double calcArea(double a, double b, double c) {
    double s = (a + b + c) / 2;
    return sqrt(s * (s - a) * (s - b) * (s - c));
}

void input() { scanf("%lf%lf%lf", &a, &b, &c); }

void process() {
    if (isTriangle(a, b, c))
        printf("%.2f\n", calcArea(a, b, c));
    else
        printf("No solution.\n");
}

int main() {
    input();
    process();
    return 0;
}
```

## 复杂度分析
- 时间复杂度：O(1)
- 空间复杂度：O(1)
