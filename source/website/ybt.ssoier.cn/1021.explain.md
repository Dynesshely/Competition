# 1021 打印字符 题解

## 题目链接

- [一本通 1021](https://ybt.ssoier.cn/problem_show.php?pid=1021)

## 题目大意

输入一个整数（ASCII 码），输出对应的字符。

## 解题思路

字符和整数在 C++ 中可以互相转换。直接将整数赋值给 `char` 类型变量即可得到对应的 ASCII 字符。

## 代码实现

读入 `int` 变量 `in`，定义 `char` 变量 `l` 并赋值为 `in`（隐式类型转换），然后输出 `l`。

## 复杂度分析

- 时间复杂度：O(1)
- 空间复杂度：O(1)
