#include <bits/stdc++.h>
using namespace std;
const int maxn = 4000;
int N, M, a[maxn], b[maxn], t[maxn];
int main(){
    scanf("%d%d", &N, &M);
    for(int i = 0; i < M; ++ i)
        scanf("%d%d", &a[i], &b[i], &t[i]);
    
    return 0;
}
