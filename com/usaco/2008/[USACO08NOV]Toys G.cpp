#include <bits/stdc++.h>
using namespace std;

int D, N1, N2, C1, C2, Tc, T[100100];
int main(){
    scanf("%d %d %d %d %d %d", &D, &N1, &N2, &C1, &C2, &Tc);
    for(int i = 1; i <= D; ++ i)
        scanf("%d", &T[i]);
    return 0;
}